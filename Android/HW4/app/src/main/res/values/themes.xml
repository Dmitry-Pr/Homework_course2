<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HW4" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="RoundButton">
        <item name="android:background">@drawable/rounbutton</item>
        <item name="android:layout_width">100sp</item>
        <item name="android:layout_height">100sp</item>
        <item name="android:text">@string/round_button</item>
    </style>
    <style name="AnotherTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/another_light_primary</item>
        <item name="android:windowBackground">@color/another_background</item>
        <item name="android:textColor">#FF4081</item>
        <item name="android:fontFamily">@font/alfa_slab_one</item>
    </style>
    <style name="Theme.HW4" parent="Base.Theme.HW4" />
</resources>